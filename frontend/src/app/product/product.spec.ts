import { ProductModel, ProductFilterModel, GtinModel, BrickModel, ClassModel, FamilyModel, SegmentModel } from './product.model';

describe('ProductFilterModel', () => {
  it('should create an instance', () => {
    expect(new ProductFilterModel()).toBeTruthy();
  });
});

describe('ProductModel', () => {
  it('should create an instance', () => {
    expect(new ProductModel()).toBeTruthy();
  });
});

describe('GtinModel', () => {
  it('should create an instance', () => {
    expect(new GtinModel()).toBeTruthy();
  });
});

describe('BrickModel', () => {
  it('should create an instance', () => {
    expect(new BrickModel()).toBeTruthy();
  });
});

describe('ClassModel', () => {
  it('should create an instance', () => {
    expect(new ClassModel()).toBeTruthy();
  });
});

describe('FamilyModel', () => {
  it('should create an instance', () => {
    expect(new FamilyModel()).toBeTruthy();
  });
});

describe('SegmentModel', () => {
  it('should create an instance', () => {
    expect(new SegmentModel()).toBeTruthy();
  });
});
